/*On créé une constante d'environnement exportable afin de pouvoir connecter a tout moment a une autre Api*/

export const environment = {
    production: false,
    apiUrl: 'http://localhost:8080' // <-- ici le backend
};